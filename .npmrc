# The front-end carried peer-dependency conflicts that yarn tolerated (e.g.
# @gocardless/react-dropin declaring a React <=18 peer while the app is on React
# 19). npm is stricter by default; this keeps yarn-equivalent behaviour so a
# single root `npm install` / `npm ci` resolves the whole workspace tree.
legacy-peer-deps=true
